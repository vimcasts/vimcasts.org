$(document).foundation();$(document).ready(function() {  if (matchMedia(Foundation.media_queries['medium']).matches) {    $('#shownotes').addClass('active');  }  activateVisibleSearchField();});function activateVisibleSearchField() {  var fields = $('.swiftype-search-form input');  if (fields.size() == 1) {    // homepage and /categories page have one featured search field    fields.attr("id", "st-search-input")  } else {    // all other pages have a search field in the navigation    if (matchMedia(Foundation.media_queries['large']).matches) {      // if large grid is active, activate the first search field      fields.filter(':first').attr("id", "st-search-input");    } else {      // otherwise, activate the second search field      fields.filter(':last').attr("id", "st-search-input");    }  }};
